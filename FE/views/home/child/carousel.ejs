<div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
  <ol class="carousel-indicators" id="indicator"></ol>
  <div class="carousel-inner" id="slider"></div>
  <a
    class="carousel-control carousel-control-prev"
    href="#carouselExampleIndicators"
    role="button"
    data-slide="prev"
  >
    <i class="fa fa-angle-left" aria-hidden="true"></i>
    <span class="sr-only">Prev</span>
  </a>
  <a
    class="carousel-control carousel-control-next"
    href="#carouselExampleIndicators"
    role="button"
    data-slide="next"
  >
    <i class="fa fa-angle-right" aria-hidden="true"></i>
    <span class="sr-only">Next</span>
  </a>
</div>

<script>
  $(document).ready(function () {
    LoadCarousel();
  });

  function LoadCarousel() {
    $.ajax({
      url: "/slider",
      type: "get",
      success: function (data) {
        let idx = 0;
        data.sliders.forEach((slider) => {
          let item = '';
          let indicatior = '';
          if(idx == 0){
            item += '<div class="carousel-item active">';
              first = false;
              indicatior = ' <li data-target="#carouselExampleIndicators" data-slide-to="'+idx+'" class="active" ></li>';
          }else{
            item += '<div class="carousel-item">';
            indicatior = ' <li data-target="#carouselExampleIndicators" data-slide-to="'+idx+'"></li>';
          }
          idx++;
          $('#indicator').append(indicatior);

          item += ' <a href="">';
          item +=
            '<img class="d-block w-100"src="'+slider.img_link+'"/></a>';
          item += "</div>";
          $('#slider').append(item);
        });
      },
    });
  }
</script>
